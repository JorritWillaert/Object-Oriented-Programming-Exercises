package iterators;

public interface Iterator {
	
	public boolean hasNext();
	
	public Object next(); //Returns the element & moves to the next element (just as in the commonly used Java Iterator class)
	
}
